<%= semantic_form_for @timetable do |f| %>
  <%= f.input :base, :as => :select, :collection => Timetable::CITIES %>
  <%= f.input :destination, :as => :select, :collection => Timetable::CITIES %>
  <%= f.submit %>
<% end %>
